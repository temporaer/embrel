FIND_PROGRAM( MXMLC_BINARY mxmlc "/usr/local/flex/bin" )
IF( MXMLC_BINARY )
	CONFIGURE_FILE( ${CMAKE_CURRENT_SOURCE_DIR}/obj/PointplotConfig.xml
		${CMAKE_CURRENT_BINARY_DIR}/pointplot.xml)
	ADD_CUSTOM_COMMAND ( OUTPUT  ${CMAKE_CURRENT_BINARY_DIR}/abstree.swf   
		COMMAND ${MXMLC_BINARY} -load-config+=pointplot.xml 
		DEPENDS src/main.as
		WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}
		VERBATIM
		)
	ADD_CUSTOM_TARGET ( abstree DEPENDS ${CMAKE_CURRENT_BINARY_DIR}/abstree.swf )
ENDIF( MXMLC_BINARY )
